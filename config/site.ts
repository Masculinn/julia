export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Julia | Web Engine",
  description:
    "Create your own website by just dragging & dropping the UI blocks with Julia.",
  navItems: [
    {
      label: "Home",
      href: "/",
    },
  ],
  navMenuItems: [],
  links: {},
};
